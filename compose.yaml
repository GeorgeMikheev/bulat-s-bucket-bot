services:
  db:
    image: postgres:latest
    command: postgres -c 'max_connections=200'
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    volumes:
      - postgresql-data:/var/lib/postgresql/data
    restart: on-failure

  nginx:
    command: nginx -g "daemon off;"
    depends_on:
      - app
    image: nginx:alpine
    restart: on-failure
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - static:/var/www/app/static
      - media:/var/www/app/media
    ports:
      - "8000:8000"

  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    command: bash -c 'while !</dev/tcp/db/5432; do sleep 1; done; python3 manage.py collectstatic --no-input; python3 manage.py migrate; uvicorn core.asgi:application --port 8000 --host 0.0.0.0'
    volumes:
      - ./src/:/app/
      - static:/app/static
      - media:/app/media
    depends_on:
      - db
    restart: on-failure
    env_file:
      - .env

  cron:
    build:
      context: .
      dockerfile: docker/DockerfileCron
    command: bash -c "printenv > /etc/environment; crontab crontab; cron && tail -f /var/log/cron.log"
    depends_on:
      - db
    volumes:
      - ./src/:/app/
    restart: on-failure
    env_file:
      - .env


volumes:
  postgresql-data:
  static:
  media:
